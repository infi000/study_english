1. 在本地电脑准备 Dockerfile
在您的 Next.js 项目根目录下，创建一个名为 Dockerfile 的文件（无后缀），内容请直接复制这段生产环境优化版：

Dockerfile
# 使用 Node 18 运行环境
FROM node:18-alpine AS runner
WORKDIR /app

# 设置环境变量为生产模式
ENV NODE_ENV production

# 这里的逻辑是：假设您已经在本地运行过 npm run build
# 我们只把必要的文件拷贝进镜像，这样镜像体积最小
COPY package.json package-lock.json ./
RUN npm install --production

COPY .next ./.next
COPY public ./public
COPY next.config.js ./ 

EXPOSE 3000
CMD ["npm", "start"]
2. 本地执行构建 (Build)
在您的电脑终端（Terminal 或 PowerShell）进入项目目录，执行以下命令：

先在本地完成项目打包：

Bash
npm install
npm run build
构建 Docker 镜像：

Bash
# 注意末尾有一个点，代表当前目录
docker build -t my-nextjs-app:v1 .
3. 将镜像导出为文件 (Save)
构建完成后，我们需要把这个镜像“打包”成一个可以传输的文件：

Bash
docker save -o my-nextjs-app.tar my-nextjs-app:v1
执行完后，您的文件夹里会出现一个 my-nextjs-app.tar 文件。